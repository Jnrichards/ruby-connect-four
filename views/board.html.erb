<!DOCTYPE html>
<html>
  <head>
    <title>Connect Fout</title>
    <link href="/assets/css/bootstrap.min.css" rel="stylesheet" media="screen">
    <link href="/assets/css/application.css" rel="stylesheet" media="screen">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="container">
      <section id="game">
        <div>
          <div>
            <div class="page-header">
              <h1>Connect Four</h1>
            </div>
            <table class="table grid">
              <tbody>
              <% 5.step(0, -1) do |i| %>
                <tr>
                <% for j in 0..6 %>
                    <td class="">
                      <% val = board.fetch(j.to_s, {}).fetch(i.to_s, {}) %>
                      <div class="slot <% if val == 1 %>filled<% elsif val == 2 %>filled-opponent<% end %>"></div>
                    </td>
                <% end %>
                </tr>
              <% end %>
              </tbody>
            </table><br>
            <div class="alert alert-info">
              <p>Waiting for opponent<span class="pull-right">Peer ID: 18qd8f6f9iugcik9</span></p>
            </div>
          </div>
        </div>
      </section>
      <section id="chat">
        <div class="page-header">
          <h1>Chat</h1>
        </div>
        <form id="input-form" class="form-inline">
          <input id="input-text" type="text" class="form-control" placeholder="Enter chat text here!" autofocus />
          <input id="input-player" type="hidden" value="<%= player %>"/>
          <button class="btn btn-primary" type="submit">Send</button>
        </form>
        <div id="chat-text"> </div>
      </section>
    </div>

    <script type="text/javascript" src="/assets/js/jquery-2.0.3.min.js"></script>
    <script type="text/javascript" src="/assets/js/application.js"></script>
  </body>
</html>
